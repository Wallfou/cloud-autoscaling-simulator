# Experiments

- Four workloads × **rr** and **lc**: low constant rate, high constant, burst, sine.
- Three autoscaling presets × **rr** and **lc** on high load: aggressive (scale sooner), conservative (scale later), shorter cooldown.

Same **seed (42)**, **duration (800)**, and **cost-rate (0.001)** within the script so comparisons are fair.
